{
  "__cvs__" = "$Id$";

  requires = {
    bundleManagerVersion = 1;
    classes = (
      { name = NSObject;    }
    );
  };

  provides = {
    classes = (
      { name = "SkyMasterApplication"; },
      { name = "SkyMasterAction";      },
    );
  };
}
