{
  CVS = "$Id$";

  requires = {
    bundleManagerVersion = 1;
    classes = (
      { name = NSObject;    }
    );
  };

  provides = {
    classes = (
      { name = "SkySystemApplication"; },
      { name = "SkySystemAction";      },
    );
  };
}
