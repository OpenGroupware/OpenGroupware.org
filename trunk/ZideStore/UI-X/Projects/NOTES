# $Id$

Project UI Notes
================

"Fast" Project Attributes:
... not so much ...
  projectId	[X] (link)
  ownerId
  teamId
  name
  number	[X]
  startDate	[X]
  endDate	[X]
  isFake
  status
  dbStatus
  kind
  url
- we could cache comment based on version
- how long does it take to fetch 100 projects?
- for the duration of an hour or so we could cache a whole box-card
  - query id+version, check permissions, then use the cache
- use attributes for fetching owner
