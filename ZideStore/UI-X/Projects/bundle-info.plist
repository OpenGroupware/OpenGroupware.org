{
  "__cvs__" = "$Id$";

  requires = {
    bundleManagerVersion = 1;
    classes = (
      { name = NSObject;    }
    );
  };

  provides = {
    ZideStoreProducts = ( { name = ProjectsUIProduct; } );
    
    classes = (
    );

    WOComponents = (
    );
  };
}
