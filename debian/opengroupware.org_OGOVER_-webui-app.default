#Startup configuration for the OpenGroupware.org application server
#This file will be sourced by /etc/init.d/opengroupware.org-webui

#If set "false", the application server won't be started on boot,
#regardless of the runlevel settings.
START_AT_BOOT=true

#Arguments to passed to the application server. Change these values only
#if you know what you are doing!
ARGS="-WOPort 20000 -WOHttpAllowHost localhost"
