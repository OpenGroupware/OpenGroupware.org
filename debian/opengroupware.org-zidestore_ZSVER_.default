#Startup configuration for the OpenGroupware.org zidestore server
#This file will be sourced by /etc/init.d/opengroupware.org-zidestore

#If set "false", the zidestore server won't be started on boot,
#regardless of the runlevel settings.
START_AT_BOOT=true

#Arguments to passed to the zidestore server. Change these values only
#if you know what you are doing!
ARGS="-WOPort 21000 -WOHttpAllowHost localhost"
