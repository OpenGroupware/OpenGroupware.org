# $Id$

skydbd
======

This daemon allows you to operate on a SQL database using XML-RPC. Certainly
not the fastest way to work with databases, but sufficient for inserts and
small queries.
Has the huge advantage that the client system is not required to have the
database vendors client libraries installed.
